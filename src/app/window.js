import { Dimensions } from 'react-native';

let { height, width, scale } = Dimensions.get("window");

const paddingHorizontalAndVertical = 15

export { height, width, scale, paddingHorizontalAndVertical };